# 变更记录

## 2026-09-22 — 长期规划确认（仅文档）

- 保存未来 6—12 个月的产品与技术路线、阶段验收及 G0—G10 候选目标；未创建或执行 Goal。
- 将测试分发前移到 M0，明确角色体验与提醒原型并行，完整日用版仍需双角色。
- 明确本地无账号、用户控制安静策略、Rust 提醒状态与唯一持久化写入者、独立气泡窗口等待实施边界。
- 补充试用的继续／调整／暂停条件、轻量专注与任务边界、公开分发前置条件。
- 按远端证据更新架构 CI 为已通过；GUI 实测保持待完成，M0 未关闭。未修改运行代码、依赖或工作流。

## 未发布 — 长期计划与架构基础

- 明确角色陪伴、三类日常提醒、两个内置角色与双平台朋友试用的长期计划；计划功能尚未实现。
- 将动画状态、角色参数、运行调度与 Tauri/DOM/时钟适配分离，入口只负责装配。
- 增加可停止的生命周期，忽略停止后的异步结果，捕获拖动失败；验证 IPC 坐标。
- 增加入口行为、状态时序、生命周期与适配器回归，增加依赖边界检查。
- CI 增加结构检查、Rust fmt/test/Clippy；真实运行与 GUI 验收独立记录，不因配置存在而标记通过。
- 继承 Windows 单屏基线记录，保留原 Codex 封存包。

## 2026-09-18 — 独立应用 v0.2.0

### 完成

- 改用窗口位置变化作为拖动中的权威信号，不再依赖不稳定的 WebView 拖动结束时序。
- 拖动过程中完全屏蔽注视方向帧，消除眼神抖动。
- 向右拖动播放 `running-right`，向左拖动播放 `running-left`。
- 上下拖动沿用最近一次水平方向，停止 160 ms 后恢复鼠标注视。
- 10 项前端测试、Rust 测试、Clippy 和前端构建通过。
- 用户在 Apple Silicon macOS 上实机确认所有拖动行为正常。

## 2026-09-18 — 独立应用 v0.1.1

### 修复

- 拖动宠物时暂停注视方向采样，避免鼠标坐标与窗口原点异步更新导致眼神抖动。
- 拖动结束后等待 100 ms 再恢复追踪，避免窗口坐标尚未稳定时跳帧。

## 2026-09-18 — 独立应用 v0.1.0

### 完成

- 将应用正式命名为 **March 7th**。
- 在 `march-7th-app/` 创建 Tauri 2 + TypeScript + Rust 项目。
- 完成 Apple Silicon macOS 的透明、无边框、始终置顶窗口。
- 复用已封存的 v2 图集，实现六帧待机和真实鼠标 16 方向跟随。
- 建立 Windows x64 `GetCursorPos` 平台实现和 macOS/Windows CI 矩阵。
- 前端方向映射 7 项测试、Rust 单元测试、Clippy 和 Apple Silicon Release 构建通过。

### 尚未实现

- 点击穿透和交互模式切换
- 托盘菜单、开机启动、位置持久化
- Windows 实机验证与安装包
- macOS 签名及公证

## 2026-09-17 — v1.0

### 完成

- 创建“三月七”Codex v2 动画宠物。
- 完成 9 个标准动画状态。
- 完成 4 个基准方向和 16 个顺时针视线方向。
- 对“失败”动作行进行一次完整重生成，修复空帧提取问题。
- 单独生成向左移动行，避免镜像导致相机和服装配件换侧。
- 重做第 10 行方向动画，修复 `292.5 / 315 / 337.5` 错误转向画面右侧的问题。
- 再次重做第 10 行，修复 `315` 靠近最终单元格边缘的问题。
- 通过 v2 图集验证、去色键验证、方向盲测和独立最终视觉 QA。
- 安装到 `~/.codex/pets/march-7th/`。

### 当前已知警告

- `022.5` 与 `337.5` 的近正上方水平提示较弱，脱离顺序单独观察时可能被判断为水平轴不明确。
- `180 → 202.5` 与 `315 → 337.5` 的像素变化量较大，但正常尺寸下没有明显跳变。
- 连续性工具报告的透明孔洞位于腿部和裙摆间，为预期负空间。

### 下一步候选

- 在不破坏顺序连续性的前提下增强两个近正上方向的左右提示。
- 进一步平滑头部转向幅度。
- 在宠物尺寸仍清晰的范围内恢复参考图中的细小服装纹样。
