# 自动化协作者规范

开始前阅读 [CONTRIBUTING.md](CONTRIBUTING.md)、[公开协作规则](docs/COLLABORATION.md)、[产品计划](docs/development/app/PRODUCT-PLAN.md) 与 [架构说明](docs/development/app/ARCHITECTURE.md)。这些公开工程约束同样适用于人工贡献者。

## 执行与证据

- 记录实际分支、完整提交 SHA、目标、可修改范围和停止边界；一个任务交付一个可验证结果。
- 不把其他分支、旧 TODO 或历史报告的结论当作当前 HEAD 的完成状态。
- 保留现有分层：纯规则在 domain，调度在 application，平台调用在 adapters；角色表现不能拥有业务进度。Rust 的状态和持久化边界遵循架构规范。
- 沿用项目检查命令和同提交双平台验收约定；报告实际通过、失败、跳过和未执行内容。不要伪造工具日志或实机结果。
- PR 公开说明目标、变更、取舍、验证和限制。完整会话、临时提示词、本机环境细节与内部接力流水不新增到公开仓库。
- 不访问与任务无关的凭据；不在文档、测试数据或日志中加入密钥和未脱敏个人信息。

## 变更边界

不覆盖他人未提交工作，不强推默认分支，不移动发布标签，不修改封存的 `releases/v1.0.0`。独立分支和 PR 不等于合并或发布授权；素材权利、付费投入和产品验收的既有确认要求保持不变。

旧记录需迁往受限空间时，先验证目的地权限与原件读回，再修改公开原件；目的地不可用时保留原件并明确阻塞。公开目录或普通 Git 分支不能用来存放需要保密的新记录。
