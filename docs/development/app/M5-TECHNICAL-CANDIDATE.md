# M5 独立技术候选与验收缺口

状态：技术候选已上传至[草稿 PR #27](https://github.com/szm20060312/march-7th-desktop-pet/pull/27)，尚未合入 `main`；不是 M4 价值试用结论、M5 产品验收或公开发布许可。2026-09-23 的功能代码基线为 `a1b301f`，已验证的原生包完整提交为 `4cb11dd29cfa152571a743833ba7a09ea9e939b4`。后续提交及测试包仍须分别核对自己的完整提交号。

## 已实现的技术范围

- **M5-A 专注陪伴**：Rust 只管理一个会话，支持开始、暂停、继续、提前结束、放弃、睡眠/时钟异常恢复和一次性自然完成；设置页可手动操作。专注进行时日常提醒仍到期并保留待处理，但自动气泡静默。自然完成在允许的活动时段、非全局稍后且无手动提醒视图时，复用现有气泡提示一次；否则结果留在固定入口。完成气泡可查看合并的待处理数，不补弹每个历史周期。两角色沿用现有动作和短句。
- **M5-B 当前一件事**：会话开始可选一个短任务名，完成/放弃各只反馈一次；任务结果与专注计时独立，不保存任务列表、项目或统计。`focus.json` 内层 v1 可迁移到 v2；外层四项数据集/备份维持 v2，旧备份可读。任务正文只在设置页和用户主动导出的本地备份里；导入预览、默认诊断及角色响应不带名称。
- **M5-C 分发技术准备**：已有未签名 Windows 当前用户 NSIS 与 Mac Apple Silicon DMG 的原生 CI 候选流程、完整提交/程序/安装包哈希核对、同提交配对及依赖许可清单。它保留原来的可直接运行回归包，不创建公开 Release、自动更新、签名或付费资源。

架构边界是：前端只做表现与非权威秒数插值；专注进度由 Rust 单一服务负责，状态先原子写入成功再发布。四项配置（位置、角色、提醒、专注）由一个活动数据集指针整体选择；旧 v1 数据与备份有显式迁移及保护。提醒和专注共享一个气泡呈现协调器，角色目录不承载计时或数据写入。细节见 [架构说明](ARCHITECTURE.md)、[数据集迁移](M5-DATASET-V2.md)、[当前一件事](../../M5-CURRENT-TASK.md) 与 [分发准备](DISTRIBUTION-READINESS.md)。

## 已取得与尚缺的证据

| 证据 | 当前结果 | 不能推出 |
|---|---|---|
| 本地自动检查 | 源码基线 `a1b301f`：前端 199 项、Rust 214 单元＋28 集成、当时打包脚本 22 项、目录锁 2 项；架构、类型、Vite、格式、严格 Clippy 通过。NSIS 标记修复后的本地打包脚本为 23 项，前端检查仍为 199 项 | 原生 GUI、睡眠/显示器、真人体验 |
| 浏览器布局 | 设置页 460×620、320×500、260×360；完成气泡 320×300、240×180，均使用隔离的内存宿主检查可达性 | Tauri 原生首次点击、焦点或穿透 |
| 独立源码检查 | 专注呈现与当前任务各做一次针对性只读检查，未发现可行动缺陷 | Windows/macOS 同提交构建或产品价值 |
| [应用 CI](https://github.com/szm20060312/march-7th-desktop-pet/actions/runs/35852834996) | `4cb11dd` 的 Windows/macOS 检查均成功 | 桌面 GUI 或用户价值 |
| [原生构建 CI](https://github.com/szm20060312/march-7th-desktop-pet/actions/runs/35852829974) | `4cb11dd` 的 Windows/macOS 回归包、未签名 NSIS/DMG 候选与同提交配对均成功；四份产物已下载，核对完整提交、clean 身份、SHA-256、PE x64/Mach-O ARM64 及 Mac 执行权限 | 签名、公证或安装/升级/回退可用 |

**仍需进行**：所有者在 Windows、协作者在 Mac 对 `4cb11dd` 同提交测试包按同一清单实测窗口、角色、提醒、专注、任务、文件对话框、重启恢复、睡眠与升级回退。Mac Spaces 与先前一次配置目录锁的间歇失败仍须实证排查。所有者还需验收两位角色动作和短句，并用约两周的少量朋友试用判断“提醒有用且不烦”及任务相对纯计时的额外价值。素材分发范围、签名、预算和维护责任未确定前不公开发布。

此前 `03af006` 包尚未由 Windows 所有者实测，当前集中回归以 `4cb11dd` 的同提交包为准；更晚的文档提交不会把旧包的测试结果自动改写为新提交结果。历史 CI、旧版 Mac 状态栏实测和浏览器预览不能代填这次结果。验收步骤见 [共用回归清单](regression/CHECKLIST.md) 与 [分发候选清单](distribution-candidate/CHECKLIST.md)。
